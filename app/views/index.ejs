<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Semantic Search - Brainia</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary-color: #4285f4;
            --bg-color: #ffffff;
            --text-color: #202124;
            --text-secondary: #70757a;
            --border-color: #dfe1e5;
            --shadow: 0 1px 6px rgba(32,33,36,0.28);
            --card-hover: #f8f9fa;
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
            background-color: var(--bg-color);
            color: var(--text-color);
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            align-items: center;
            min-height: 100vh;
        }

        header {
            width: 100%;
            padding: 20px 0;
            display: flex;
            flex-direction: column;
            align-items: center;
            border-bottom: 1px solid var(--border-color);
            margin-bottom: 20px;
        }

        .logo {
            font-size: 2.5rem;
            font-weight: 700;
            margin-bottom: 20px;
            cursor: pointer;
            text-decoration: none;
            color: var(--text-color);
        }

        .logo span:nth-child(1) { color: #4285F4; }
        .logo span:nth-child(2) { color: #EA4335; }
        .logo span:nth-child(3) { color: #FBBC05; }
        .logo span:nth-child(4) { color: #4285F4; }
        .logo span:nth-child(5) { color: #34A853; }
        .logo span:nth-child(6) { color: #EA4335; }

        .search-container {
            width: 90%;
            max-width: 600px;
            position: relative;
        }

        .search-form {
            width: 100%;
            display: flex;
            align-items: center;
            background: #fff;
            border: 1px solid var(--border-color);
            border-radius: 24px;
            padding: 5px 20px;
            transition: box-shadow 0.2s;
            flex-wrap: wrap; /* Allow wrapping for filters */
        }

        .filters-row {
            width: 100%;
            display: flex;
            gap: 10px;
            margin-top: 10px;
            padding-top: 10px;
            border-top: 1px solid #f1f3f4;
            flex-wrap: wrap; /* Tràn hàng khi có nhiều filter */
        }

        .filter-select {
            padding: 8px 12px;
            border: 1px solid #dfe1e5;
            border-radius: 4px;
            background: #f8f9fa;
            font-size: 13px;
            color: #3c4043;
            cursor: pointer;
            outline: none;
            min-width: 150px;
            flex-grow: 1; 
        }

        .filter-select:hover {
            box-shadow: 0 1px 1px rgba(0,0,0,0.1);
            background: #fff;
            border-color: #dadce0;
        }

        .search-form:hover, .search-form:focus-within {
            box-shadow: var(--shadow);
            border-color: transparent;
        }

        .search-input {
            flex-grow: 1;
            border: none;
            padding: 10px;
            font-size: 16px;
            outline: none;
        }

        .search-button {
            background: none;
            border: none;
            cursor: pointer;
            padding: 5px;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        main {
            width: 90%;
            max-width: 700px;
            padding-bottom: 40px;
        }

        .results-info {
            color: var(--text-secondary);
            font-size: 14px;
            margin-bottom: 20px;
        }

        .result-item {
            margin-bottom: 30px;
            animation: fadeIn 0.5s ease;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(10px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .result-link {
            text-decoration: none;
            display: block;
        }

        .result-title {
            color: #1a0dab;
            font-size: 20px;
            font-weight: 500;
            margin: 0 0 4px 0;
        }

        .result-link:hover .result-title {
            text-decoration: underline;
        }

        .result-meta {
            font-size: 12px;
            color: var(--text-secondary);
            margin-bottom: 4px;
            display: flex;
            gap: 10px;
            align-items: center;
        }

        .score-badge {
            background: #e6f4ea;
            color: #1e8e3e;
            padding: 2px 10px;
            border-radius: 12px;
            font-weight: 600;
        }

        .meta-tag {
            background: #f1f3f4;
            color: #5f6368;
            padding: 2px 10px;
            border-radius: 12px;
            font-weight: 500;
        }

        .result-snippet {
            font-size: 14px;
            line-height: 1.58;
            color: #4d5156;
            word-wrap: break-word;
        }

        .no-results {
            text-align: center;
            margin-top: 50px;
            color: var(--text-secondary);
        }

        @media (max-width: 600px) {
            .logo { font-size: 2rem; }
            .search-container { width: 95%; }
        }
    </style>
</head>
<body>

<header>
    <a href="/" class="logo">
        <span>B</span><span>r</span><span>a</span><span>i</span><span>n</span><span>i</span><span>a</span>
    </a>
    <div class="search-container">
        <form action="/search" method="GET" class="search-form">
            <input type="text" name="q" class="search-input" placeholder="Tìm kiếm theo ngữ nghĩa (vd: Nhân viên sale biết tiếng Anh)..." value="<%= query %>">
            <button type="submit" class="search-button">
                <svg focusable="false" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" width="24" height="24"><path d="M15.5 14h-.79l-.28-.27A6.471 6.471 0 0 0 16 9.5 6.5 6.5 0 1 0 9.5 16c1.61 0 3.09-.59 4.23-1.57l.27.28v.79l5 4.99L20.49 19l-4.99-5zm-6 0C7.01 14 5 11.99 5 9.5S7.01 5 9.5 5 14 7.01 14 9.5 11.99 14 9.5 14z"></path></svg>
            </button>
        
            <!-- Filters Section -->
            <div class="filters-row">
                <!-- Location Filter -->
                <select name="city_id" class="filter-select">
                    <option value="">-- Tất cả địa điểm --</option>
                    <% Object.keys(constants.locationMap).forEach(id => { %>
                        <option value="<%= id %>" <%= filters && filters.city_id == id ? 'selected' : '' %>><%= constants.locationMap[id] %></option>
                    <% }); %>
                </select>

                <!-- Salary Filter -->
                <select name="s" class="filter-select">
                    <option value="">-- Mức lương --</option>
                    <% Object.keys(constants.salaryMap).forEach(id => { %>
                        <option value="<%= id %>" <%= filters && filters.salary == id ? 'selected' : '' %>><%= constants.salaryMap[id] %></option>
                    <% }); %>
                </select>

                <!-- Experience Filter -->
                <select name="e" class="filter-select">
                    <option value="">-- Kinh nghiệm --</option>
                    <% Object.keys(constants.expMap).forEach(id => { %>
                        <option value="<%= id %>" <%= filters && filters.experience == id ? 'selected' : '' %>><%= constants.expMap[id] %></option>
                    <% }); %>
                </select>

                <!-- Category (Ngành nghề) -->
                <select name="c" class="filter-select">
                    <option value="">-- Ngành nghề --</option>
                    <% Object.keys(constants.categoryMap).forEach(id => { %>
                        <option value="<%= id %>" <%= filters && filters.category == id ? 'selected' : '' %>><%= constants.categoryMap[id] %></option>
                    <% }); %>
                </select>

                <!-- Level (Cấp bậc) -->
                <select name="k" class="filter-select">
                    <option value="">-- Cấp bậc --</option>
                    <% Object.keys(constants.levelMap).forEach(id => { %>
                        <option value="<%= id %>" <%= filters && filters.level == id ? 'selected' : '' %>><%= constants.levelMap[id] %></option>
                    <% }); %>
                </select>

                <!-- Work Type (Loại hình) -->
                <select name="t" class="filter-select">
                    <option value="">-- Loại hình --</option>
                    <% Object.keys(constants.typeMap).forEach(id => { %>
                        <option value="<%= id %>" <%= filters && filters.work_type == id ? 'selected' : '' %>><%= constants.typeMap[id] %></option>
                    <% }); %>
                </select>

                <!-- Gender (Giới tính) -->
                <select name="g" class="filter-select">
                    <option value="">-- Giới tính --</option>
                    <% Object.keys(constants.genderMap).forEach(id => { %>
                        <option value="<%= id %>" <%= filters && filters.gender == id ? 'selected' : '' %>><%= constants.genderMap[id] %></option>
                    <% }); %>
                </select>
            </div>
        </form>
    </div>
</header>

<main>
    <% 
        // Helper function inside EJS to map arrays or single values
        const getLabel = (val, map) => {
            if (Array.isArray(val)) return val.map(id => map[id]).filter(Boolean).join(', ');
            return map[val] || '';
        };
    %>

    <% if (results && results.length > 0) { %>
        <div class="results-info">Tìm thấy <%= results.length %> ứng viên tiềm năng</div>
        
        <% results.forEach(item => { %>
            <div class="result-item">
                <div style="display: flex; gap: 20px;">
                    <!-- Avatar -->
                    <div style="flex-shrink: 0;">
                        <% if (item.dbData.photo) { %>
                            <img src="<%= item.dbData.photo %>" style="width: 65px; height: 65px; border-radius: 8px; object-fit: cover; border: 1px solid #eee;">
                        <% } else { %>
                            <div style="width: 65px; height: 65px; border-radius: 8px; background: #f1f3f4; display: flex; align-items: center; justify-content: center; font-size: 24px;">👤</div>
                        <% } %>
                    </div>

                    <!-- Details -->
                    <div style="flex-grow: 1;">
                        <a href="/candidate/<%= item.dbData.id %>" class="result-link">
                            <h3 class="result-title"><%= item.dbData.name %></h3>
                        </a>
                        
                        <div style="font-size: 15px; font-weight: 500; color: #1a73e8; margin-bottom: 8px;">
                            🎯 <%= item.dbData.position %>
                        </div>

                        <div class="result-meta">
                            <span class="score-badge">Độ khớp: <%= (item.score * 100).toFixed(1) %>%</span>
                            <% if (item.payload) { %>
                                <span class="meta-tag">📍 <%= getLabel(item.payload.city_id, constants.locationMap) || 'Toàn quốc' %></span>
                                <span class="meta-tag">💰 <%= constants.salaryMap[item.payload.salary] || 'Thỏa thuận' %></span>
                                <span class="meta-tag">⏳ <%= constants.expMap[item.payload.experience] || 'Chưa có' %></span>
                                <% const levelLabel = getLabel(item.payload.level, constants.levelMap); %>
                                <% if (levelLabel) { %><span class="meta-tag">🎓 <%= levelLabel %></span><% } %>
                                <% const typeLabel = getLabel(item.payload.work_type, constants.typeMap); %>
                                <% if (typeLabel) { %><span class="meta-tag">🕒 <%= typeLabel %></span><% } %>
                                <% const genderLabel = getLabel(item.payload.gender, constants.genderMap); %>
                                <% if (genderLabel) { %><span class="meta-tag">👤 <%= genderLabel %></span><% } %>
                            <% } %>
                        </div>

                        <% if (item.dbData.about) { %>
                            <div style="font-size: 14px; color: #3c4043; margin-top: 8px; line-height: 1.5;">
                                <strong>Giới thiệu:</strong> <%= item.dbData.about %>
                            </div>
                        <% } %>

                        <% const mappedSkillsLabel = getLabel(item.dbData.skills, constants.skillsMap); %>
                        <% if (mappedSkillsLabel) { %>
                            <div style="font-size: 13px; color: #1967d2; background: #f8f9fa; display: inline-block; padding: 4px 10px; border-radius: 4px; margin: 10px 0; border: 1px solid #e8f0fe;">
                                🛠️ <strong>Kỹ năng:</strong> <%= mappedSkillsLabel %>
                            </div>
                        <% } %>

                        <% if (item.dbData.skill_content) { %>
                            <div class="result-snippet" style="margin-top: 10px; color: #4d5156; font-size: 14px;">
                                <strong>Kinh nghiệm & Kỹ năng chi tiết:</strong><br>
                                <%= item.dbData.skill_content.substring(0, 500) %><%= item.dbData.skill_content.length > 500 ? '...' : '' %>
                            </div>
                        <% } %>
                    </div>
                </div>
            </div>
        <% }) %>
    <% } else if (query) { %>
        <div class="no-results">
            <img src="https://www.gstatic.com/images/branding/googlelogo/2x/googlelogo_color_92x30dp.png" style="opacity: 0.2; margin-bottom: 20px;">
            <p>Không tìm thấy ứng viên nào phù hợp với yêu cầu của bạn.</p>
        </div>
    <!-- <% } 
    else { %>
        <div class="no-results" style="padding: 100px 20px;">
            <div style="font-size: 60px; margin-bottom: 20px;">🔍</div>
            <p style="font-size: 18px;">Nhập thế mạnh ứng viên hoặc vị trí công việc để bắt đầu.</p>
        </div> -->
    <% } %>
</main>

</body>
</html>
